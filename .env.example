# Copy to .env and fill in real values. Never commit .env.

# Database
DATABASE_URL=postgresql://saiap:saiap_dev_password@db:5432/saiap

# Redis
REDIS_URL=redis://redis:6379/0

# Auth
JWT_SECRET_KEY=change_me_to_a_long_random_value
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# SSO / OIDC (fill in once resolved — PRD open question 1)
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=

# MFA
MFA_ISSUER_NAME=SAIAP
